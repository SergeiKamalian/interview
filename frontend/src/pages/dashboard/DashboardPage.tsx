import { Alert, Card } from '@shared/ui';

export function DashboardPage() {
  return (
    <Card header="Company dashboard">
      <Alert variant="info" title="Placeholder">
        Protected dashboard routes and data will be implemented in block 04+.
      </Alert>
    </Card>
  );
}
