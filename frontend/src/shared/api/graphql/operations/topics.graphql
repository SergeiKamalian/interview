query Topics($skillId: String, $professionId: String) {
  topics(skillId: $skillId, professionId: $professionId) {
    id
    code
    name
    interviewWeight
    isCustom
    skill {
      id
      code
      name
      isCustom
    }
  }
}
