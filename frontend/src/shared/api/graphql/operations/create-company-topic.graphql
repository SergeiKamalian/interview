mutation CreateCompanyTopic($input: CreateCompanyTopicInput!) {
  createCompanyTopic(input: $input) {
    id
    code
    name
    interviewWeight
    isCustom
    skill {
      id
      code
      name
    }
  }
}
