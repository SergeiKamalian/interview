mutation UpdateCompanyTopic($input: UpdateCompanyTopicInput!) {
  updateCompanyTopic(input: $input) {
    id
    code
    name
    interviewWeight
    isCustom
    skill {
      id
      code
      name
    }
  }
}
